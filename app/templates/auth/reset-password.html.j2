{% extends 'auth.html.j2' %}

{% block content %}
<form method="post">
    {{ form.hidden_tag() }}

    <div class="uk-margin">
        {% if form.password.errors %}
        {{ form.password(class_='uk-input uk-form-danger') }}
        {% for error in form.password.errors %}
        <small class="uk-form-help-block">{{ error }}</small>
        {% endfor %}
        {% else %}
        {{ form.password(class_='uk-input', placeholder='Password') }}
        {% endif %}
    </div>

    <div class="uk-margin">
        {% if form.confirm_password.errors %}
        {{ form.confirm_password(class_='uk-input uk-form-danger') }}
        {% for error in form.confirm_password.errors %}
        <small class="uk-form-help-block">{{ error }}</small>
        {% endfor %}
        {% else %}
        {{ form.confirm_password(class_='uk-input', placeholder='Confirm password') }}
        {% endif %}
    </div>

    <div class="uk-margin">
        {{ form.submit(class='uk-button uk-button-primary') }}
    </div>
</form>
{% endblock %}
